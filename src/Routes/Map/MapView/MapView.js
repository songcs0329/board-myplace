import React from 'react';
import { MapViewStyles } from '../MapStyles';

const MapView = () => {
  return (
    <MapViewStyles>
      <div id="map"></div>
    </MapViewStyles>
  );
};

export default MapView;