import React from 'react';
import { MapStyles } from './MapStyles';

const Presenter = () => {
  return (
    <MapStyles>
      <div id="map"></div>
    </MapStyles>
  );
};

export default Presenter;