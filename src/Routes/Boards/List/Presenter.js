import React from 'react';
import { BoardListStyles } from './ListStyles';

const Presenter = () => {
  return (
    <BoardListStyles>
      BoardListPresenter
    </BoardListStyles>
  );
};

export default Presenter;